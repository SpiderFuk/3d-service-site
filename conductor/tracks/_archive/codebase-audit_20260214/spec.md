# Specification: Codebase Audit & Cleanup

**Track ID:** codebase-audit_20260214
**Type:** Refactor
**Created:** 2026-02-14
**Status:** Draft

## Summary

Full codebase audit and cleanup to address accumulated tech debt. Eliminate `any` types, convert remaining Svelte 4 patterns to Svelte 5 runes, extract duplicated code into shared utilities, and remove deprecated exports.

## Context

[PRINTO] is a SPA for a 3D printing service built with SvelteKit 5, TypeScript, Tailwind CSS, and Three.js. The codebase has grown organically and while overall quality is high (A- grade), there are specific areas with accumulated tech debt that need cleanup before building new features.

### Audit Findings Summary

| Severity | Count | Description |
| --- | --- | --- |
| Critical | 0 | None |
| Moderate | 4 | `any` types (8 files), Svelte 4 reactive statements, code duplication (2 instances) |
| Minor | 2 | Deprecated exports, console statements (acceptable) |

## Acceptance Criteria

- [ ] All `any` types replaced with proper TypeScript types (8 files)
- [ ] `Services.svelte` converted from `$:` reactive statements to `$derived()` runes
- [ ] Duplicated `enhanceMaterials()` extracted to shared utility
- [ ] Duplicated placeholder loading logic extracted to shared utility
- [ ] Deprecated legacy exports removed from `filamentColors.ts` (if unused)
- [ ] `npm run check` passes with zero errors
- [ ] `npm run build` succeeds
- [ ] Bundle size equal or smaller than before

## Dependencies

None

## Out of Scope

- Store modernization to Svelte 5 runes (separate future track)
- Adding a logging service to replace console statements
- Major Three.js restructuring (conservative approach)
- Adding tests (no test framework configured yet)
- Accessibility improvements (already in good shape)

## Technical Notes

- **Be conservative with Three.js code** - core feature, avoid breaking rendering or interactions
- Three.js material types from `@types/three` should be used for `material: any` parameters
- The `Snippet` type from `svelte` should replace `any` for `children` props
- `Component` type from `svelte` should replace `any` for icon maps

### Files Affected

| File | Issues |
| --- | --- |
| `src/routes/+layout.svelte` | `children?: any` |
| `src/lib/components/ui/Card.svelte` | `children?: any` |
| `src/lib/components/ui/Button.svelte` | `children?: any` |
| `src/lib/components/sections/Services.svelte` | `iconMap: Record<string, any>`, `$:` reactive statements |
| `src/lib/components/sections/ModelViewer.svelte` | `material: any`, duplicated `enhanceMaterials()` and placeholder loading |
| `src/lib/components/viewer/FileUploader.svelte` | `material: any`, duplicated `enhanceMaterials()` and placeholder loading |
| `src/lib/components/viewer/ModelThumbnails.svelte` | `loadedPreviews: Record<string, any>` |
| `src/lib/utils/fileValidation.ts` | `as any` cast |
| `src/lib/config/filamentColors.ts` | Deprecated legacy exports |

---

_Generated by Conductor. Review and edit as needed._
